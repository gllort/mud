#include <arma.h>

void arma() {
    nombre("$N$4Ira de Cain$R");
    descripcion("Es una espada.");
    genero(FEMENINO);
    volumen(100);    
    tipo_daño(({ CORTANTE, APLASTANTE }));
    daño(10,20);
    critico(15);
    multiplicador(2);
}
